<resources>
    <!-- Base application theme for dark mode. Renamed to match the app manifest's theme so night values are applied. -->
    <style name="Theme.NT118_EnglishVocabApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Night-mode primary brand color (override via values-night/colors.xml if you prefer). -->
        <item name="colorPrimary">@color/light_purple</item>
        <item name="colorPrimaryVariant">@color/light_purple</item>
        <!-- Use white on dark backgrounds for proper contrast in night mode -->
        <item name="colorOnPrimary">@android:color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>