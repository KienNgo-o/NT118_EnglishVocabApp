<?xml version="1.0" encoding="utf-8"?>
<!-- Pronounce-specific copy of item_vocab.xml so the two fragments can diverge later -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp"
    android:layout_marginStart="8dp"
    android:layout_marginEnd="8dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="8dp"
    android:background="@drawable/bg_topic_both_rounded">

    <!-- Container for word, type and favorite icon -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:id="@+id/word_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_alignParentStart="true">

            <!-- Word (main title) -->
            <TextView
                android:id="@+id/txt_word"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:text="Word"
                android:textColor="@color/card_text_color"
                android:textSize="20sp"
                android:textStyle="bold" />

            <!-- Phonetic spelling next to the word -->
            <TextView
                android:id="@+id/txt_phonetic"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:text="/fəˈnɛtɪk/"
                android:textColor="@android:color/darker_gray"
                android:textSize="14sp" />
        </LinearLayout>

    </RelativeLayout>

    <!-- Short definition (allow up to 2 lines) -->
    <TextView
        android:id="@+id/txt_definition"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:text="A short definition of the word goes here."
        android:textSize="14sp"
        android:textColor="@color/card_text_color"
        android:layout_marginTop="6dp"
        android:maxLines="2"
        android:ellipsize="end" />

</LinearLayout>
