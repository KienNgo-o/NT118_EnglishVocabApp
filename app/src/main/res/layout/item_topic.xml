<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp"
    android:layout_marginStart="8dp"
    android:layout_marginEnd="8dp"
    android:background="@drawable/bg_topic_rect">

    <!-- Word (main title) -->
    <TextView
        android:id="@+id/txt_word"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Word"
        android:textSize="20sp"
        android:textColor="@color/dark_purple"
        android:textStyle="bold" />

    <!-- Word type (noun/verb/etc.) -->
    <TextView
        android:id="@+id/txt_word_type"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="(n.)"
        android:textSize="14sp"
        android:textColor="@android:color/darker_gray"
        android:textStyle="italic"
        android:layout_marginTop="4dp" />

    <!-- Short definition (allow up to 2 lines) -->
    <TextView
        android:id="@+id/txt_definition"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="A short definition of the word goes here."
        android:textSize="14sp"
        android:textColor="@color/dark_purple"
        android:layout_marginTop="6dp"
        android:maxLines="2"
        android:ellipsize="end" />

    <!-- Divider between cards -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="8dp"
        android:background="@android:color/darker_gray" />

</LinearLayout>
